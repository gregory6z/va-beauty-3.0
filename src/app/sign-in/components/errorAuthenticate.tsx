// Supondo que useAuthStore seja o nome do seu hook customizado de autenticação

export const ErrorAuthenticateDisplay = () => {
  // const error = useAuthStore.getState().authError

  return <p className="text-sm text-red-500">{}</p>
}
